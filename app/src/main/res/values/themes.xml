<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal platform theme. Jetpack Compose provides the real (Material 3) theming
  at runtime (see ui/theme/Theme.kt); this XML theme only needs to supply a
  no-action-bar window with a dark background so there is no white flash before
  Compose draws.
-->
<resources>
    <style name="Theme.ImplantDoom" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
